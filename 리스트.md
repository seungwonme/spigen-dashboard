맞아요, 쪼개서 복잡하게 만들었네요. 처음부터 끝까지 **한 줄기로** 정리합니다.

# 전체 흐름 (슈피겐 대시보드에 시트+Gmail+Calendar+Drive 붙이기)

### 0. 관리자 (한 번, 슈피겐 admin)
- `admin.google.com → 앱 → 추가 Google 서비스 → Google Cloud Platform = ON`
- GCP 조직 정책 `iam.disableServiceAccountKeyCreation` = 미적용
→ 이거 두 개면 학생들이 gcloud로 다 할 수 있음.

### 1. API 켜기 (gcloud 한 줄)
```
gcloud services enable sheets.googleapis.com gmail.googleapis.com calendar-json.googleapis.com drive.googleapis.com docs.googleapis.com --project=spigen-497602
```

### 2. 시트 연결 — 서비스 계정 (이미 키 발급 완료)
- `.env.local`에 `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PRIVATE_KEY` 있음
- **시트에 그 SA 이메일을 뷰어로 공유** → 끝
- 코드: `/api/sheets` route가 서비스 계정으로 시트 읽음

### 3. Gmail/Calendar/Drive 권한 — OAuth (콘솔에서)
1. `API 및 서비스 → OAuth 동의 화면` → **내부** 선택 → scope 4개 등록
2. `사용자 인증 정보 → OAuth 클라이언트 ID → 웹 애플리케이션`
   - 리디렉션 URI: `https://<프로젝트>.supabase.co/auth/v1/callback`
   - → **Client ID / Secret** 복사
3. `Supabase → Authentication → Providers → Google` → Client ID/Secret 붙여넣기

### 4. 사이트 연결 — 코드
- 로그인 시 권한 요청:
```ts
supabase.auth.signInWithOAuth({
  provider: "google",
  options: {
    scopes: "https://www.googleapis.com/auth/gmail.send https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/drive.file",
    queryParams: { access_type: "offline", prompt: "consent" },
  },
});
```
- 로그인 후 `session.provider_token`으로 Gmail/Calendar/Drive 호출 (서버 route에서)

---

**정리:** 관리자 2개 허용 → gcloud로 API 켜기 → 시트는 서비스계정(공유만) → Gmail/Calendar/Drive는 OAuth(동의화면+클라이언트+Supabase) → 코드에서 시트 읽고 권한토큰으로 API 호출.

이대로 코드 파일(`/api/sheets` + Gmail/Calendar/Drive route)까지 실제로 만들어드릴까요?
